package com.tory.rednov.controller;

import com.tory.rednov.model.AppSettings;

public class AppSettingsListener implements Listener {
    @Override
    public void update(Object o) {
        AppSettings appSettings = (AppSettings) o;
        //TODO update the view
    }
}
